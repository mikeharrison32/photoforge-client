export function getPixelsWithinCustomSelection(
  points: number[],
  canvas: HTMLCanvasElement
): number[] {
  return points;
}

/**  Replace a set of pixels by another set of pixels
 * @param canvas canvas to operate on
 * @param source the source of pixels to use for replacing
 */
export function replacePixels(
  canvas: HTMLCanvasElement,
  source: any,
  dest: any
) {}
