import { DestroyableDirective } from './destroyable.directive';

describe('DestroyableDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyableDirective();
    expect(directive).toBeTruthy();
  });
});
